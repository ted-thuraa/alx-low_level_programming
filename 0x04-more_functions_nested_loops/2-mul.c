#include "main.h"

/**
 * mul - rints product of a and b
 *
 * @a: first digit
 * @b: second digit
 *
 * Description: Uses headers to link and a nested loops to achieve goal
 * Return: product of a and b
 */

int mul(int a, int b)
{
	return(a * b);
}
