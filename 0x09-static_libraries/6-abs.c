#include "main.h"

/**
 * _abs - computes the abs value
 * @n: an int that takes in a number from main
 * Return: int 0 when done and no errors
 */
int _abs(int n)

{
	if (n >= 0)

		return (n);
	else
		return (-n);
}

